# Since the ".env" file is gitignored, you can use this file to create a local
# `.env`. Keep it up to date when app env requirements change.

# Frontend API origin used by the Next.js app
NEXT_PUBLIC_API_URL=http://localhost:3001

# Optional personal site URL used in UI links
NEXT_PUBLIC_RESUME_URL=
